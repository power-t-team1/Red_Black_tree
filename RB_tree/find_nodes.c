#include "common.h"
/*
int find_nodes(tree_t *root)
{
	//Base case and validation for one node with LC and RC NULL
     if (root == NULL)
     {
         return 0;
     }

     //Traversing and returing the total number of nodes available in the list
     return (find_nodes(root->LC) + 1 + find_nodes(root->RC));
}*/
